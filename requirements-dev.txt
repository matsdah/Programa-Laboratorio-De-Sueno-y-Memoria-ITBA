# Herramientas de desarrollo. Instalar además de requirements.txt:
#     pip install -r requirements.txt -r requirements-dev.txt
#
# Las dependencias exclusivas de la Parte 2 (módulo de análisis) NO están acá:
# viven en requirements-analysis.txt, porque son pesadas y porque el techo
# numpy<2.6 que necesita numba no tiene por qué alcanzar a la Parte 1. Pero hay
# que instalarlas igual para correr la suite entera: desde el hito 10 hay tests
# que las importan.

-r requirements.txt

pytest>=8.0             # MIT    - testeos recurrentes por componente
pip-licenses>=4.3       # MIT    - verificación de licencias de las dependencias
